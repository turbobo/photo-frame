# 框影 · Framelet — 技术方案文档

> 版本：v1.5.0 · 最后更新：2026-08-22

---

## 一、项目定位

框影是一款**纯浏览器端**的相机照片边框水印工具，为国内用户提供以下能力：

- 快速为照片添加**摄影师风格边框**（极简/拍立得/胶片/EXIF 参数栏/社交卡片）
- 自动读取 EXIF 元数据（相机型号、镜头、光圈、快门、ISO、日期、GPS）
- 自动匹配 14 个主流相机品牌 Logo（Sony/Canon/Nikon/Fujifilm/Leica...）
- 支持 **RAW / JPG / PNG / HEIC** 全部主流拍摄格式
- 一键导出多种尺寸和格式

**目标用户**：摄影爱好者、旅行博主、社交媒体分享（小红书/微博/朋友圈）

---

## 二、技术选型

| 维度 | 选型 | 版本 | 理由 |
|------|------|------|------|
| 构建工具 | **Vite** | 5.4 | 冷启动毫秒级、HMR 极快、生态完整 |
| 前端框架 | **React** | 18.3 | 组件生态、Hook API 心智负担低 |
| 类型系统 | **TypeScript** | 5.4 | 严格模式、Vite 原生支持 |
| 样式方案 | **Tailwind CSS** | 3.4 | 原子化、体积小、暗色主题友好 |
| EXIF 解析 | **exifr** | 7.1 | 支持 RAW 内嵌预览、GPS、XMP、覆盖 20+ 格式 |
| 图像处理 | **Canvas 2D API** | 浏览器原生 | 零依赖、DPR 缩放、导出 blob |
| ZIP 打包 | **fflate** | 0.8 | 8KB gzip、流式 API、零依赖 |
| 部署平台 | **EdgeOne Pages** | 免费版 | 国内 CDN 加速、静态托管、免费 10GB/月 |

**为什么不选 Next.js + Sharp**（对照方案 B）：
- EdgeOne Pages 免费版**无 Node 运行时**，Sharp 无法运行
- Sharp 的 RAW 支持需要 libraw 编译，二进制 100MB+，超过 Serverless 免费额度
- 图片处理放浏览器 = **零服务器带宽 + 零上传等待 + 用户隐私更好**

---

## 三、格式支持策略

### 3.1 支持清单

| 类别 | 格式 | 处理方式 |
|------|------|---------|
| 常规位图 | JPG/JPEG · PNG · WEBP · AVIF | Canvas 原生 `Image` 加载 |
| 移动端 | HEIC/HEIF（iPhone 拍摄） | exifr 提取内嵌 JPEG 预览 |
| **RAW** | CR2/CR3（Canon）· NEF（Nikon）· ARW/SR2（Sony）· RAF（Fujifilm）· RW2（Panasonic）· ORF（Olympus）· PEF（Pentax）· DNG（通用）· RWL（Leica） | exifr 提取内嵌 full-resolution JPEG |

### 3.2 RAW 处理原理

RAW 文件内**都嵌有一张 full-resolution JPEG 预览**（相机屏幕回放用）。exifr 直接读取这张 JPEG 用于渲染，不需要解 Bayer 传感器数据（那需要 dcraw/libraw，无法在纯浏览器实现）。

**优势**：
- 秒开（提取只需读 IFD 段，< 500ms）
- 无需服务器
- 相机厂商已经调过色，视觉效果良好

**局限**：
- 不能做 RAW 显影（曝光、白平衡等参数）
- 预览分辨率一般接近原图但不完全等同

### 3.3 关键代码

`src/utils/exif.ts::loadImage()`：

```ts
import * as exifr from 'exifr'

export async function loadImage(file: File): Promise<HTMLImageElement> {
  validateImageFile(file)
  if (isNativeImage(file)) return loadImageFromBlob(file)

  const preview = await exifr.thumbnail(file)
  if (!preview) throw new Error('无法提取图片预览')
  return loadImageFromBlob(new Blob([preview], { type: 'image/jpeg' }))
}
```

`exifr` 采用静态导入以避免部署环境动态 chunk 加载失败；实际实现还包含分块失败后的全量读取回退，以及解码后尺寸校验。

---

## 四、边框模板架构

### 4.1 二十一种模板

| 分组 | ID | 名称 | 视觉特征 | 适用场景 |
|------|----|------|---------|---------|
| 基础 | `minimal`  | 极简    | 白/黑边 + 底部一行 EXIF 小字             | Instagram Story, 日常分享 |
| 基础 | `polaroid` | 拍立得  | 上左右窄边 + 底部大留白 + 手写风签名     | 复古文艺、日系 |
| 品牌 | `leica`    | 徕卡栏  | 底部黑栏 + 红点 + LEICA + WETZLAR        | 徕卡用户、高端展示 |
| 品牌 | `red-dot`  | 红点水印| 悬浮右下角（不扩画布）+ 半透明黑底 + 红点 | 徕卡/索尼用户、极简 |
| 品牌 | `exif`     | 参数栏  | 底部深色栏 · Logo + 型号 + 参数          | 街拍、专业 |
| 胶片 | `film`     | 胶片    | 黑框 + 齿孔 + 帧号 + EXIF                 | 摄影发烧友 |
| 胶片 | `dazz`     | Dazz 胶卷| 135 胶卷边框 + 竖排文字 + 日期背印      | 胶片复古、Instagram |
| 胶片 | `instax`   | Instax  | 真实拍立得比例 + 米白底 + 纸张纹理        | 旅行记录、手帐 |
| 胶片 | `vintage`  | 复古纸相框| 牛皮纸底 + 噪点纹理 + sepia 滤镜       | 文艺复古、VSCO 风 |
| 社交 | `insta`    | 社交卡片| 白底毛玻璃 + 圆角 + 阴影                 | 小红书、朋友圈 |
| 社交 | `xhs`      | 小红书  | 3:4 白底卡片 + 顶部标签 + 标题描述        | 小红书笔记封面 |
| 社交 | `magazine` | 杂志封面| 顶部 PHOTO ZINE 大标题 + 底部分隔 caption | 时尚摄影、作品展示 |
| 社交 | `location` | 地理水印| 底部栏 · Logo + 型号 + 📍地点 + 日期      | 旅行摄影、地理打卡 |
| 基础 | `light-shadow` | 光影 | 底部纯黑薄条 + 单行 EXIF | 简洁参数展示 |
| 基础 | `frameless-rounded` | 无框圆角 | 圆角图片 + 悬浮阴影 | 极简展示 |
| 基础 | `white-border` | 白色边框 | 经典相框 + 双栏 EXIF | 摄影作品展示 |
| 品牌 | `ps-splash` | PS 启动窗 | Photoshop 风格启动界面 | 创意分享 |
| 品牌 | `lr-splash` | LR 启动窗 | Lightroom 风格启动界面 | 创意分享 |
| 特效 | `vintage-photo` | 老照片 | 数码管时间戳 + 发光 | 怀旧影像 |
| 特效 | `text-embed` | 文字内嵌 | 半透明 EXIF 覆盖 | 信息化展示 |
| 特效 | `tiled-watermark` | 平铺水印 | 可调密度、角度和透明度 | 版权保护 |

### 4.2 渲染统一入口

`src/utils/canvas.ts`：

```ts
export function renderFrame(ctx: RenderCtx): HTMLCanvasElement {
  switch (ctx.config.id) {
    case 'minimal':  return renderMinimal(ctx)
    case 'polaroid': return renderPolaroid(ctx)
    case 'film':     return renderFilm(ctx)
    case 'exif':     return renderExif(ctx)
    case 'insta':    return renderInsta(ctx)
    case 'leica':    return renderLeica(ctx)
    case 'red-dot':  return renderRedDot(ctx)
    case 'dazz':     return renderDazz(ctx)
    case 'instax':   return renderInstax(ctx)
    case 'xhs':      return renderXhs(ctx)
    case 'vintage':  return renderVintage(ctx)
    case 'magazine': return renderMagazine(ctx)
    case 'location': return renderLocation(ctx)
    case 'light-shadow':      return renderLightShadow(ctx)
    case 'frameless-rounded': return renderFramelessRounded(ctx)
    case 'white-border':      return renderWhiteBorder(ctx)
    case 'ps-splash':         return renderPsSplash(ctx)
    case 'lr-splash':         return renderLrSplash(ctx)
    case 'vintage-photo':     return renderVintagePhoto(ctx)
    case 'text-embed':        return renderTextEmbed(ctx)
    case 'tiled-watermark':   return renderTiledWatermark(ctx)
    default:                  return renderMinimal(ctx)
  }
}
```

每个渲染器接收：
- `image: HTMLImageElement` — 原图
- `exif: ExifData` — 元数据
- `logo: HTMLImageElement | null` — 品牌 Logo
- `config: TemplateConfig` — 用户参数（padding/color/fontSize...）
- `seed?: number` — 可选纹理种子；未传入时按「模板 + 型号 + 日期 + 背景色 + 图片尺寸」推导

`TemplateConfig` 字段：

```ts
interface TemplateConfig {
  id: TemplateId           // 21 种模板之一
  padding: number          // 边距 0-30 (%)
  bgColor: string          // 边框颜色
  textColor: string        // 文字颜色
  showLogo: boolean        // 显示品牌 Logo + 型号
  showExif: boolean        // 显示拍摄参数 + 日期
  customText: string       // 自定义文字（覆盖默认）
  fontSize: number         // 字号相对图片长边百分比 1-5
  logoSize: number         // Logo 相对图片长边百分比 2-10
  radius: number           // 圆角 0-80 px
  shadow: boolean          // 卡片阴影
  hideEmptyExif?: boolean  // 无任何文案时是否隐藏信息区（默认 true）
  locationName?: string    // 地点名（location 模板专用）
  xhsImageFit?: 'contain' | 'cover'  // 小红书封面填充方式
  magazineName?: string    // 杂志刊名（默认 PHOTO ZINE）
  magazineIssue?: string   // 杂志期号（默认取日期后四位）
}
```

返回：一个 `HTMLCanvasElement`，用于预览与导出。

资源与并发安全约定：
- 上传前按扩展名白名单校验格式，拒绝空文件及超过 200 MiB 的单文件。
- 图片解码后限制最大边长 12000 像素、总像素 6000 万，避免异常图片触发超额内存和 Canvas 分配。
- 每次选图分配递增请求序号，过期异步结果不得覆盖最新照片。
- `HTMLImageElement` 加载完成后立即释放 Object URL。
- 单张和批量导出均在编码完成或失败后清空临时 Canvas；用户取消批量任务时不下载残缺 ZIP。

### 4.3 渲染分层

渲染核心拆为三个文件，避免「文案决策」与「排版实现」耦合：

| 文件 | 职责 |
|------|------|
| `utils/frameContent.ts` | **内容解析层**：统一开关语义、变量替换、空值判定、纯图回退 |
| `utils/renderUtils.ts` | **排版与材质工具**：字号适配、省略号截断、确定性随机、图片填充 |
| `utils/canvas.ts` | **渲染器实现**：21 个模板具体绘制，只消费上面两层的结果 |

**统一开关语义**（所有模板必须一致）：

| 字段 | 控制范围 |
|------|---------|
| `showExif` | 拍摄参数（焦距/光圈/快门/ISO）与拍摄日期 |
| `showLogo` | 品牌 Logo 图片与型号/镜头文字 |
| `customText` | 非空时始终优先展示，并替换默认文案 |
| `hideEmptyExif` | 无任何可展示内容时，带独立信息栏的模板回退为纯图输出 |

`resolveFrameContent(exif, config, logo)` 返回 `FrameContent`：
`model / lens / exifLine / date / params[] / custom / title / subtitle / hasAny / shouldHideInfo / logo`。

两类模板的回退策略：

- **自带相框类**（minimal / polaroid / instax / insta）：保留边框，仅省略文字；
- **独立信息栏类**（exif / leica / location / magazine / film / dazz / white-border /
frameless-rounded / xhs）：无内容时收起信息栏，直接输出纯图。

### 4.4 确定性纹理

复古纸相框与 PS/LR 亚克力噪点使用 `createSeededRandom`（mulberry32）而非 `Math.random`，
种子由「模板 + 型号 + 日期 + 背景色 + 图片尺寸」哈希得到，保证**预览、切换参数、单张导出、
批量导出得到完全相同的颗粒效果**。

### 4.5 固定画幅

`fitImageInBox(srcW, srcH, boxW, boxH, mode)` 处理固定画幅模板的图片落位：

- `contain`：整图完整保留，短边留白；
- `cover`：等比放大后居中裁切填满。

小红书模板据此输出**严格 3:4** 封面（宽高比常量 `XHS_COVER_RATIO`），
图片填充方式由 `xhsImageFit` 控制（默认 `cover`，可在面板切换为「完整保留」）。

### 4.6 尺寸与字号策略

所有尺寸参数使用**图片长边百分比**表达，确保不同分辨率下视觉一致：

```ts
const long = Math.max(W, H)
const pad  = long * config.padding / 100      // 边距 5% → 20MP 图片是 200px
const font = long * config.fontSize / 100     // 字号 2%  → 约 100px
```

---

## 五、UI 架构

### 5.1 三栏布局

```
┌──────────────────────────────────────────────┐
│  Header: Logo + 支持格式徽章                  │
├──────┬────────────────────────┬──────────────┤
│ 模板 │      Canvas 预览        │ Tabs        │
│ 5 项 │      （棋盘格背景）      │ 样式/信息/  │
│      │                        │ 导出         │
└──────┴────────────────────────┴──────────────┘
```

### 5.2 组件树

```
App
├─ PhotoUploader（拖拽/粘贴/点击上传，支持 compact 变体）
├─ TemplateSelector（左栏，5 个模板卡片）
├─ PhotoPreview（中间，Canvas + 自适应缩放 + DPR）
└─ 右栏 Tabs
   ├─ TemplateEditor（滑块、颜色、开关、自定义文字）
   ├─ ExifInfoPanel（EXIF 元数据展示）
   └─ ExportPanel（尺寸 4 档 + 格式 3 种 + 质量滑块）
```

### 5.3 交互特性

- **拖拽**：整个上传区支持 dragover/drop，视觉反馈 dropzone-active
- **粘贴**：全局监听 Cmd+V，从剪贴板获取图片
- **实时预览**：config 每次变化 → useEffect 重绘 canvas
- **DPR 适配**：预览 canvas 使用 devicePixelRatio 高清渲染
- **自适应缩放**：ResizeObserver 监控容器变化，长边自动限制在可视区

---

## 六、数据流

```
User 上传 File
    ↓
loadImage(file)   ─┐
extractExif(file) ─┼→ 并行 (Promise.all)
    ↓              │
    ├─ HTMLImageElement (原图/RAW 预览)
    └─ ExifData → getLogoPath(make) → loadLogo()
    ↓
setPhoto({ file, image, exif })  →  App state
    ↓
config 变化时 → renderFrame({image, exif, logo, config})
    ↓
返回 HTMLCanvasElement → 传给 PreviewCanvas 用 drawImage 缩放显示
    ↓
导出：re-renderFrame → toBlob(mime, quality) → download
```

---

## 七、性能预算

| 指标 | 实测 | 目标 |
|------|------|------|
| 首屏 JS gzip | 55 KB (index) | < 200 KB ✅ |
| exifr gzip | 约 26 KB（静态包含） | 避免部署环境动态 chunk 加载失败 ✅ |
| CSS gzip | 4 KB | < 15 KB ✅ |
| 冷启动 (dev) | 380ms | < 1s ✅ |
| RAW 预览提取 | ~300ms（20MP 相机） | < 500ms ✅ |
| Canvas 重绘 | ~40ms（20MP 图 + 5 模板） | < 50ms ✅ |
| 生产构建 | 546ms | < 3s ✅ |

**优化措施**：
- exifr 静态导入，避免 EdgeOne 部署环境下 dynamic import chunk 获取失败
- Logo 使用 SVG（矢量、体积小、缓存友好）
- Canvas 一次性渲染，预览用 drawImage 二次缩放（避免重复计算）
- Vite 生产模式自动 tree-shaking、esbuild 压缩和资源哈希

---

## 八、批量导出

### 8.1 核心挑战

浏览器端批量渲染照片的主要瓶颈是**内存**，不是 CPU：

| 资源 | 24MP 照片 (6000×4000) | 12MP 照片 (4000×3000) |
|------|----------------------|----------------------|
| 源图 HTMLImageElement 解码 | 96 MB | 48 MB |
| 输出 Canvas（含边距） | ~121 MB | ~60 MB |
| 单次渲染峰值 | **~217 MB** | **~108 MB** |
| JPEG Blob 输出 | ~5-8 MB | ~3-5 MB |

如果同时在内存中保留所有源图 + 所有输出 canvas，10 张 24MP 照片就需要 ~2.2 GB，iOS Safari 直接崩溃。

### 8.2 方案选型

| 方案 | 峰值内存 | 最大批量 | 备注 |
|------|---------|---------|------|
| A. 全部加载后批量渲染 | N × 217 MB | ~5 张（iOS） | ❌ 不可行 |
| B. 逐张渲染 + 累积 Blob | 217 MB + N × 8 MB | ~70 张（iOS） | ⚠️ Blob 累积 |
| **C. 逐张渲染 + ZIP 分块缓冲** | **单图峰值 + ZIP 输出体积** | **20-50 张** | ✅ 采用；避免最终整包二次复制 |

### 8.3 架构设计

```
用户点击「批量导出 ZIP」→ 选择多张照片
    ↓
ControlPanel 启动 runBatchExport()
    ↓
for each file (串行):
    loadImage(file) + extractExif(file)  [并行]
    getLogoPath → loadLogo              [缓存命中]
    renderFrame(ctx)                     [Canvas 2D]
    canvas.toBlob(mime, quality)         [编码]
    blob → arrayBuffer → fflate ZIP 流   [写入]
    ── 清理 ──
    canvas.width = 0                     [释放 GPU 内存]
    URL.revokeObjectURL(image.src)       [释放 Object URL]
    ↓
zip.end() → 等待 final 回调 → chunks 直接构造 Blob → 触发下载
    ↓
BatchProgressModal 保留完成状态并报告成功/失败
```

### 8.4 各平台安全上限

通过 `navigator.deviceMemory`（Chrome/Edge/Android）和 UA 检测（iOS）动态设置：

| 平台 | 可用内存 | 安全上限 | 检测方式 |
|------|---------|---------|---------|
| Chrome Desktop | ~2 GB | **50 张** | `deviceMemory ≥ 4` |
| iOS Safari（iPhone 13+） | ~1 GB | **20 张** | UA 匹配 iPhone/iPad |
| Android 旗舰（8GB+） | ~1.5 GB | **30 张** | `deviceMemory ≥ 4` + Android UA |
| Android 低端（2-3GB） | ~500 MB | **20 张** | `deviceMemory < 4` |

### 8.5 内存清理策略

每张照片处理完成后执行：

```ts
// 1. 释放 Canvas GPU 纹理（立即生效，不等 GC）
canvas.width = 0
canvas.height = 0

// 2. Object URL 在图片加载完成后立即释放
// 3. ZIP 输出块会保留至下载 Blob 创建完成，但不再复制成额外合并缓冲区
```

### 8.6 技术选型：fflate

| 维度 | fflate | jszip | 对比 |
|------|--------|-------|------|
| 体积 (gzip) | **8 KB** | 45 KB | 5.6x 更小 |
| 流式 API | ✅ `Zip` + `ZipPassThrough` | ❌ 全量 | fflate 支持增量写入 |
| 依赖 | 零 | 零 | 持平 |
| 压缩级别 | 可选 0-9 | 固定 | JPEG/WebP 用 level=0（已压缩，避免浪费 CPU） |

### 8.7 取消与错误处理

- **取消**：`AbortController` + `signal.aborted` 检查，用户点击"取消"立即中止循环
- **错误隔离**：每张照片独立 `try/catch`，失败记录 `{ name, reason }` 但不中断批量
- **结果汇报**：完成后弹窗显示成功/失败计数及失败详情

### 8.8 文件结构

- `src/utils/batchExport.ts` — 批量导出引擎（设备检测 + 逐张渲染 + ZIP 分块输出）
- `src/components/BatchProgressModal.tsx` — 全屏进度浮层（进度条 + ETA + 取消）

### 8.9 ZIP 文件约定

- ZIP 文件名：`framelet-batch-YYYYMMDD-HHMMSS.zip`
- 内部文件名：`{原文件名}-{模板ID}-{序号}.{格式后缀}`（序号避免同名覆盖）

---

## 九、EdgeOne 部署

### 9.1 配置文件

`edgeone.json`：

```json
{
  "outputDirectory": "dist"
}
```

### 9.2 部署流程

1. 推送 GitHub / Coding：`git push origin main`
2. EdgeOne Pages 控制台 → 「新建项目」→ 关联仓库
3. 构建命令：`npm run build`
4. 输出目录：`dist`
5. Node 版本：**20.18.0**（Vite 5 要求 ≥ 20.17）
6. 首次部署 ~2min，后续增量部署 ~30s

### 9.3 免费额度

| 项 | 额度 |
|---|---|
| 静态资源存储 | 100 GB |
| CDN 流量 | 10 GB/月 |
| 请求次数 | 10 万/月 |

对本项目而言：
- 单张照片 gzip 后约 100KB → 支持 10 万次访问/月免费

---

## 十、目录结构

```
photo-frame/
├── index.html
├── vite.config.ts
├── tailwind.config.js
├── postcss.config.js
├── tsconfig.json
├── package.json
├── edgeone.json
├── 技术方案文档.md
├── 产品设计文档.md
├── public/
│   ├── favicon.svg
│   └── logos/               14 个相机品牌 SVG
│       ├── sony.svg  canon.svg  nikon.svg
│       ├── fujifilm.svg  panasonic.svg  olympus.svg
│       ├── leica.svg  pentax.svg  hasselblad.svg
│       ├── ricoh.svg  apple.svg  huawei.svg
│       └── xiaomi.svg  dji.svg
└── src/
    ├── main.tsx                    入口
    ├── App.tsx                     三栏布局 + 状态编排
    ├── index.css                   Tailwind + 自定义动画
    ├── types.ts                    ExifData / TemplateConfig / PhotoData
    ├── components/
    │   ├── PhotoUploader.tsx       拖拽 / 粘贴 / 点击上传
    │   ├── PhotoPreview.tsx        Canvas 预览 + DPR + 自适应
    │   ├── ControlPanel.tsx        样式/信息 Tabs + 导出 + 批量导出
    │   └── BatchProgressModal.tsx  批量导出进度浮层
    ├── utils/
    │   ├── exif.ts                 loadImage + extractExif
    │   ├── logos.ts                品牌名 → Logo 路径映射 + 缓存
    │   ├── frameContent.ts       ★ 内容解析层（开关语义 + 空值判定）
    │   ├── renderUtils.ts        ★ 排版 / 确定性纹理 / 图片填充工具
    │   ├── canvas.ts             ★ 21 模板渲染核心
    │   ├── fonts.ts               字体系统 + 变量替换
    │   ├── presets.ts             预设持久化 (localStorage)
    │   ├── batchExport.ts         批量导出引擎 (fflate ZIP + 逐张渲染)
    │   └── *.test.ts              单元测试（Vitest）
    └── templates/
        └── index.ts                模板元数据 + 默认配置
```

---

## 十一、扩展路线

### v1.1 计划
- ~~批量导入 + 一键批量套模板~~ ✅ 已实现（fflate 流式 ZIP）
- ~~导出为 zip 压缩包~~ ✅ 已实现
- PWA 离线支持
- 用户自定义 Logo 上传

### v1.2 计划
- 更多模板：证件照风、Vlog 字幕框、拟物纸质相框
- GPS 地图小图（高德静态图 API）
- 分享到微信/小红书（生成分享卡片）
- WASM 版真实 RAW 解码（libraw-wasm，可选加载）

### v2.0 计划
- 用户账号 + 云端同步（云开发）
- AI 智能水印文案（OpenAI Vision → 场景描述）
- 视频封面制作

---

## 十二、已知限制

| 限制 | 原因 | 缓解 |
|------|------|------|
| 大于 100MB 的 RAW 文件浏览器可能崩溃 | 内存限制 | 提示用户先降采样 |
| Safari 不支持 dynamic import 部分场景 | 兼容性 | 已用 Vite polyfill |
| 手写风字体 fallback | 系统字体差异 | 使用 Snell Roundhand → STXingkai 链 |
| Logo 文字风格非官方 | 商标合规 | 免费使用、可用户自定义 |
| 批量导出张数受限：iOS 20 / Android 30 / Desktop 50 | 浏览器内存预算 | 动态检测 + UI 提示上限，超限自动截取 |

---

## 十三、维护 checklist

- [ ] 每次代码改动同步更新本文档
- [ ] 新增/修改用户功能同步更新 `产品设计文档.md`
- [ ] 依赖升级前跑 `npm audit`
- [ ] 提交前执行 `npm run check`（TypeScript + Vitest）
- [ ] 新增/修改模板时校验 `showExif` / `showLogo` / `hideEmptyExif` 三个语义未偏离 §4.3
- [ ] 新增随机纹理必须改用 `createSeededRandom`，禁止 `Math.random`（否则预览与导出不一致）
- [ ] EdgeOne 部署前本地跑 `npm run build` 验证

---

## 十四、模板扩充（v1.2 → v1.3）

原有 §4.1 列出 13 个基础/品牌/胶片/社交模板。v1.2-v1.3 阶段额外补齐 8 个 Copicseal 风格 + 软件封面 + 特效模板，至此 `canvas.ts::renderFrame` 共 21 个 case，与 `templates/index.ts::TEMPLATES` 一一对应。

| 分组 | ID | 名称 | 视觉特征 | 关键渲染逻辑 |
|------|----|------|---------|-------------|
| 特效 | `light-shadow` | 光影水印 | 图片上叠加半透明大号参数文字（横向 / 竖向自动切换） | 根据图片方向选 `layoutH/V`，`globalAlpha=0.35` + `multiply` 混合 |
| 基础 | `frameless-rounded` | 无框圆角 | 图片直接显示，四角大圆角 + 底部三行居中 EXIF | 仅剪裁 + 底部文字，无扩画布（或极小底部扩展） |
| 基础 | `white-border` | 宽白边 | 4% 纯白宽边 + 底部额外 6% 留白，左型号 / 右参数右对齐 | 类似极简但更宽的 padding，无卡片阴影 |
| 软件封面 | `ps-splash` | PS 启动屏 | 1:1 方形 · 左侧发光 Logo 盒（#31a8ff）+ 右侧参数表 | `renderSplash` 复用函数，品牌文本 "Ps" / "Adobe Photoshop" |
| 软件封面 | `lr-splash` | LR 启动屏 | 1:1 方形 · 左侧发光 Logo 盒（#0099ff）+ 右侧参数表 | 同上，品牌文本 "Lr" / "Adobe Lightroom" |
| 胶片 | `vintage-photo` | 老照片 | 七段数码管时间戳 + 双层发光（参考 Copicseal） | `SEVEN_SEG` 表查字形 → `fillRect` 画段 |
| 特效 | `text-embed` | 文字内嵌 | EXIF 文字半透明覆盖在图片 9 宫格位置 | 9 种 anchor + V/H 双布局 + `cleanupText` 去重 |
| 特效 | `tiled-watermark` | 平铺水印 | 全图平铺旋转水印瓦片，可调密度/角度/透明度 | sqrt 曲线调密度 + 旋转矩阵 + 全局 `globalAlpha` |

**新增共用工具**：

- `SEVEN_SEG`：七段数码管 0-9 / : 字形表（`{ a,b,c,d,e,f,g }` 布尔数组）
- `gridPosition`：9 宫格 + 中心线对齐辅助
- `drawCard`：3 层阴影绘制（仿 Copicseal soft-shadow）
- `cleanupText`：文本去重（仅清理 `·`，保留 `-` 和 `|`）

---

## 十五、SwiftUI iOS 移植（v1.3）

### 15.1 项目定位

`photo-frame-SwiftUI/` 是框影的原生 iOS 移植，目标：

- iPhone 本地跑全套 21 模板渲染（与 Web 视觉一致）
- 直接访问 Photos 图库 / Camera，无需上传下载
- 导出到相册 / 系统 Share Sheet / AirDrop

### 15.2 技术选型

| 维度 | 选型 | 理由 |
|------|------|------|
| UI 框架 | **SwiftUI** | iOS 17+ 声明式语法、@Observable |
| 渲染核心 | **CoreGraphics + UIGraphicsImageRenderer** | 替代 Canvas 2D，1:1 API 对应 |
| 图片加载 | **PhotosUI + PhotosPicker** | 原生相册访问，零权限配置 |
| EXIF 解析 | **ImageIO (`CGImageSourceCopyPropertiesAtIndex`)** | 原生 RAW / HEIC 支持 |
| 导出 | **`PHPhotoLibrary.shared().performChanges`** | 写回相册 |
| 构建配置 | **XcodeGen (`project.yml`)** | YAML 描述，避免 `.xcodeproj` 合并冲突 |

### 15.3 Canvas 2D → CoreGraphics 对应表

| Canvas 2D API | CoreGraphics / UIKit 对应 |
|--------------|---------------------------|
| `ctx.fillStyle = c` | `cg.setFillColor(c.cgColor)` |
| `ctx.fillRect(x,y,w,h)` | `cg.fill(CGRect)` |
| `ctx.save() / restore()` | `cg.saveGState() / restoreGState()` |
| `ctx.translate / rotate` | `cg.translateBy / rotate(by:)` |
| `ctx.globalCompositeOperation='multiply'` | `cg.setBlendMode(.multiply)` |
| `ctx.shadowColor / Blur` | `cg.setShadow(offset:, blur:, color:)` |
| `ctx.fillText / measureText` | `NSAttributedString.draw(at:)` + `.size()` |
| `ctx.beginPath / arc / stroke` | `UIBezierPath` + `addPath / strokePath` |
| `roundedRect` (CSS radius) | `UIBezierPath(roundedRect:cornerRadius:)` 扩展于 `CGContext+Draw.swift` |

**关键差异**：Canvas `textBaseline='middle'` + `textAlign='center'` 是内置的，SwiftUI 的 `NSAttributedString.draw(at:)` 从 **top-left** 起笔。所有居中/右对齐必须手工减去 `size.width/2` 或 `size.height/2`。这是移植过程中最容易踩的坑。

### 15.4 目录结构

```
photo-frame-SwiftUI/
├── project.yml                    XcodeGen spec，iOS 17 / Swift 5.9
├── Framelet.xcodeproj             XcodeGen 生成
├── 技术方案文档.md                 §本文档（SwiftUI 子项目视角）
├── 产品设计文档.md
└── Framelet/
    ├── FrameletApp.swift          @main 入口
    ├── Models/
    │   ├── Enums.swift            TemplateId (21 case) / TemplateGroup / FontFamily
    │   ├── PhotoData.swift        PhotoData + ExifData + TemplateMeta
    │   └── TemplateConfig.swift   21 个默认 config
    ├── Services/
    │   ├── FrameRenderer.swift    ★ 21 模板渲染核心（~1210 行）
    │   ├── ExifReader.swift       ImageIO 读 EXIF
    │   └── PhotoExporter.swift    PHPhotoLibrary 写回
    ├── Components/
    │   ├── PhotoPicker.swift      PhotosPicker 封装
    │   ├── TemplateSelector.swift 模板选择网格
    │   └── ControlPanel.swift     参数滑块 / 字体 / 颜色
    ├── Views/
    │   ├── ContentView.swift      主页面
    │   └── PreviewView.swift      Canvas 预览
    ├── Utils/
    │   ├── RenderConstants.swift  36 个共享常量
    │   └── CGContext+Draw.swift   fillRoundedRect / strokeRoundedRect 扩展
    └── Resources/
        ├── Assets.xcassets        品牌 Logo（14 个，待补全）
        └── Fonts/                 5 家族字体（待补全）
```

### 15.5 渲染器架构

`FrameRenderer.swift` 单文件 ~1210 行，主入口：

```swift
static func render(photo: PhotoData, config: TemplateConfig, scale: CGFloat) -> UIImage
```

内部 `switch config.id` 分发 21 个 `private static func`。几何计算统一策略：

```swift
let ref = max(canvasW, canvasH)        // 长边基准
let sidePad = max(ref * padPct, minPaddingRatio * ref)
var bottomPad: CGFloat = ...           // 按模板特化
var barH: CGFloat = ...                // 信息栏高度
var topArea: CGFloat = 0               // 顶部扩展（xhs / magazine）
```

**已实现 21/21 模板**：minimal · polaroid · film · exif · insta · leica · redDot · dazz · instax · xhs · vintage · magazine · location · lightShadow · framelessRounded · whiteBorder · psSplash · lrSplash · vintagePhoto · textEmbed · tiledWatermark。

### 15.6 与 Web 版的差异

| 维度 | Web | SwiftUI |
|------|-----|---------|
| 语言 | TypeScript | Swift |
| 渲染 | Canvas 2D | CoreGraphics |
| EXIF 库 | exifr | ImageIO |
| 批量 ZIP | fflate | 暂未实现 |
| 模板数 | 21 | 21（已对齐） |
| 字体 | Fontsource 本地托管 | 系统字体 fallback（自定义字体待打包） |
| 部署 | EdgeOne Pages | TestFlight / App Store |

### 15.7 已知限制（截至 v1.3-dev）

| 限制 | 缓解 |
|------|------|
| 自定义字体未打包（5 家族） | 全部 fallback 到 SF / PingFang；后续 PR 添加 ttf 资源 + `UIAppFonts` |
| 品牌 Logo 仅文本占位 | 14 个 SVG 待转 PDF 或 SF Symbols 替代 |
| 批量导出未实现 | iOS 端 Share Sheet 替代，批量 ZIP 列入 Roadmap P1 |
| Xcode 26 需手动装 iOS platform（~8.5 GB） | 已通过 `xcrun swiftc -typecheck -sdk iphoneos -target arm64-apple-ios17.0` 提前验证编译 |

